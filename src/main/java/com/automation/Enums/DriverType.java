package com.automation.Enums;

/**
 * Enum representing different types of WebDrivers and Mobile drivers supported by the framework
 */
public enum DriverType {
    FIREFOX,
    CHROME,
    EDGE,
    SAFARI,
    ANDROID_CHROME,
    ANDROID_NATIVE,
    IOS_SAFARI,
    IOS_NATIVE
}
